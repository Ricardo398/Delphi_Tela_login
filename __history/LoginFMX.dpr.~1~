program LoginFMX;

uses
  System.StartUpCopy,
  FMX.Forms,
  unit_login in 'Fontes\unit_login.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
